spring:
  application:
    #注意这里对应的是 nacos 配置管理中的 [DataId].properties（然后自动拉取对应的配置文件）
    name: order-app
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 #Nacos作为配置中心地址
        username: nacos
        password: nacos
        namespace: public
        group: DEFAULT_GROUP

